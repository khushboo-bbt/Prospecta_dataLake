-- What did the integration actually APPLY? Column names in
-- SYS_INTEGRATION_TABLE_ACTIVITY aren't documented in the pages we could
-- reach, so select everything and inspect what comes back.
SELECT * FROM sys_integration_table_activity LIMIT 20;
